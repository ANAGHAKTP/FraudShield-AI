## 2024-06-11 - Semantic Forms Pattern in App
**Learning:** The existing design system relies heavily on placeholder text and icons rather than proper semantic `<label>` tags for form inputs, creating accessibility gaps for screen readers.
**Action:** Always verify that newly added inputs contain `aria-label` tags or explicitly linked `<label>` tags to compensate for the missing semantic structures in the UI library.
