{
  "name": "fraudshield-ai",
  "version": "1.0.0",
  "description": "FraudShield AI - run all services locally with a single command",
  "scripts": {
    "dev": "concurrently -n ML,API,FRONTEND -c magenta,green,blue \"npm run dev:ml\" \"npm run dev:api\" \"npm run dev:ui\"",
    "dev:ml": "cd ml-backend-fastapi && python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload",
    "dev:api": "cd gateway-nestjs && npm run start:dev",
    "dev:ui": "cd frontend-react && npm run dev",
    "install:all": "npm install && cd gateway-nestjs && npm install && cd ../frontend-react && npm install --legacy-peer-deps"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.27",
    "concurrently": "^9.1.2",
    "postcss": "^8.5.8",
    "tailwindcss": "^4.2.1"
  },
  "dependencies": {
    "lucide-react": "^0.577.0"
  }
}
